' Things to find out about VBScript

'-----------------------------------------------------------------------------------------------------------------------------------------------
' Can we execute an external command/program/bat and 
'   wait for it to complete 
'   get a return status
'   pipe input and output
'
'
' The principal difference between Exec and Run is that 
'   Exec supports real-time output to the user (if it is a command-line program),
'   BUT we can't get ERRORLEVEL using this method. 

object.Run(strCommand, [intWindowStyle], [bWaitOnReturn])
intWindowStyle Integer value indicating the appearance of the program's window.
0	Hides the window and activates another window.
1	Activates and displays a window. If the window is minimized or maximized, the system restores it to its original size and position. An application should specify this flag when displaying the window for the first time.
2	Activates the window and displays it as a minimized window.
3	Activates the window and displays it as a maximized window.
4	Displays a window in its most recent size and position. The active window remains active.
5	Activates the window and displays it in its current size and position.
6	Minimizes the specified window and activates the next top-level window in the Z order.
7	Displays the window as a minimized window. The active window remains active.
8	Displays the window in its current state. The active window remains active.
9	Activates and displays the window. If the window is minimized or maximized, the system restores it to its original size and position. An application should specify this flag when restoring a minimized window.
10	Sets the show-state based on the state of the program that started the application.
bWaitOnReturn Boolean value indicating whether the script should wait for the program to finish executing before continuing to the next statement in your script.
True    script execution halts until the program finishes, and Run returns any error code returned by the program. 
False   (the default), the Run method returns immediately after starting the program, automatically returning 0 (not to be interpreted as an error code).

object.Exec(strCommand)
strCommand  String value indicating the command line used to run the script.
            The command line should appear exactly as it would if you typed it at the command prompt.
The Exec method returns a WshScriptExec object, 
which provides status and error information about a script run with Exec 
along with access to the StdIn, StdOut, and StdErr channels. 
The Exec method allows the execution of command line applications only. 
The Exec method cannot be used to run remote scripts. Do not confuse the Exec method with the Execute method (of the WshRemote object).
eg
Set objStdOut = WScript.StdOut
Set WshShell = CreateObject("WScript.Shell")
Set oExec = WshShell.Exec("calc")
Do While oExec.Status = 0 '0 is running and 1 is ending
     WScript.Sleep 100
Loop
objStdOut.WriteLine oExec.ExitCode
WScript.StdOut.WriteLine oExec.ExitCode
Set oExec = Nothing
Set WshShell = Nothing
Set objStdOut = Nothing


' https://www.codeproject.com/Tips/507798/Differences-between-Run-and-Exec-VBScript
'
' https://www.daniweb.com/programming/code/516406/vbscript-run-an-external-program-and-capture-the-output
'
' When executing dos shell commands (i.e. cmd) 
' you should ALWAYS  specify /c as an option. 
' This will terminate the shell on completion. 
' If you don't do this you may end up with orphan cmd.exe processes littering up memory
'
set wso = CreateObject("Wscript.Shell")
strErrorCode wso.Run "cmd /c dir /s /b d:\temp\*.jpg",True,7
WScript.StdOut.WriteLine strErrorCode ' always write to stdout, not use echo so it works in batch console mode
Set wso = Nothing

set wso = CreateObject("Wscript.Shell")
set exe = wso.Exec("cmd /c dir /s /b d:\temp\*.jpg")
Do While exe.Status = 0 '0 is running and 1 is ending
     WScript.Sleep 100
Loop
Do Until exe.StdOut.AtEndOfStream
    WScript.StdOut.WriteLine exe.StdOut.ReadLine
Loop
Do Until exe.StdErr.AtEndOfStream
    WScript.StdOut.WriteLine exe.StdErr.ReadLine
Loop
WScript.StdOut.WriteLine exe.ExitCode
Set exe = Nothing
Set wso = Nothing


set wso = CreateObject("Wscript.Shell")
set exe = wso.Exec("cmd /c dir /s /b d:\temp\*.jpg 2>&1")
Do While exe.Status = 0 '0 is running and 1 is ending
     WScript.Sleep 100
Loop
Do Until exe.StdOut.AtEndOfStream
    WScript.StdOut.WriteLine exe.StdOut.ReadLine
Loop
Do Until exe.StdErr.AtEndOfStream
    WScript.StdOut.WriteLine exe.StdErr.ReadLine
Loop
WScript.StdOut.WriteLine exe.ExitCode
Set exe = Nothing
Set wso = Nothing




set wso = CreateObject("Wscript.Shell")
set exe = wso.Exec("cmd /c dir /s /b d:\temp\*.jpg")
Do While exe.Status = 0 '0 is running and 1 is ending
     WScript.Sleep 100
Loop
WScript.StdOut.WriteLine exe.ExitCode
sout = exe.StdOut.ReadAll
WScript.StdOut.WriteLine sout
serr = exe.StdErr.ReadAll
WScript.StdOut.WriteLine serr
Set exe = Nothing
Set wso = Nothing


'-----------------------------------------------------------------------------------------------------------------------------------------------
' Can we execute another .vbs (eg vp.vbs) and 
'   wait for it to complete 
'   get a return status
'   pipe input and output

'cscript' runs entirely in the command line and is ideal for non-interactive scripts

Yes, use Run or Exec as above with plain Run, or cscript to force running in console-compatile mode

Dim objShell, exe
Set objShell = Wscript.CreateObject("WScript.Shell")
set exe=objShell.Run "TestScript.vbs 42 ""an arg containing spaces"" foo",7,True
WScript.StdOut.WriteLine exe.ExitCode
Set objShell = Nothing

Dim objShell, exe
Set objShell = Wscript.CreateObject("WScript.Shell")
set exe=objShellobjShell.Run "cscript //nologo """vbs_file"" ""parameter 1"" ""parameter 2""",7,True
WScript.StdOut.WriteLine exe.ExitCode
Set objShell = Nothing

'-----------------------------------------------------------------------------------------------------------------------------------------------
' how can we use regex in vbscript to change filenames to lose special characters like the poweshell script does
'   I guess also remove the leading filename tvsp stuff (see existing vbs script) eg "movies-action" etc
'   I guess also change the filenames to move the date to the end (see existing vbs script)
'
' REGEX
Set myRegExp = New RegExp
myRegExp.IgnoreCase = False
myRegExp.Global = True  
myRegExp.Pattern = "[^a-zA-Z0-9-_. ]+"      ' ^ means not matching
result_string = myRegExp.Replace(input_string,".") ' in this case replace all matching characters with ".", in this case all non-standard characters
Set myRegExp = Nothing

'-----------------------------------------------------------------------------------------------------------------------------------------------
' how can we change file modified,created date/time stamps in vbscript like the poweshell script does, using the date in the end of the filename
'

Set objShell = CreateObject("Shell.Application")
Set objFolder = objShell.NameSpace("C:\Scripts")
Set objFolderItem = objFolder.ParseName("File.jpg")
objFolderItem.ModifyDate = "01/01/2008 8:00:00 AM"
Set objFolderItem = Nothing
Set objFolder = Nothing
Set objShell = Nothing

Set fso = CreateObject("Scripting.Filesystemobject")
Set file = fso.GetFile(path) ' the full file path
Set objShell = CreateObject("Shell.Application")
Set objFolder = objShell.NameSpace(file.ParentFolder.Path)
objFolder.Items.Item(file.Name).ModifyDate = Now
Set objFolder = Nothing
Set objShell = Nothing
Set file = Nothing
Set fso = Nothing


    function fnModifyDateGetSetVB()
        dim objShell
        set objShell = CreateObject("shell.application")
        if (not objShell is nothing) then
            dim objFolder2
            dim ssfWINDOWS
            ssfWINDOWS = 36
            set objFolder2 = objShell.NameSpace(ssfWINDOWS)
            if (not objFolder2 is nothing) then
                dim objFolderItem
                set objFolderItem = objFolder2.ParseName("NOTEPAD.EXE")
                if (not objFolderItem is nothing) then
                    dim szReturn
                    szReturn = objFolderItem.ModifyDate
                    objFolderItem.ModifyDate = "01/01/1900 6:05:00 PM"
                end if
                set objFolderItem = nothing
            end if
            set objFolder2 = nothing
        end if
        set objShell = nothing
    end function


' all files in a folder
Set objShell = CreateObject("Shell.Application")
Set objFSO = CreateObject("Scripting.FileSystemObject")
Const FolderPath = "C:\Documents\Folder"
Call Moodify (FolderPath)
Sub Moodify(str)
     Set objFolderShell = objShell.NameSpace(str)
     Set objFolderFSO = objFSO.GetFolder(str)
     Set colFiles = objFolderFSO.Files
     For Each objFile in colFiles
             Set objFolderItem = objFolderShell.ParseName(objFile.Name)
             objFolderItem.ModifyDate = "04/11/2013 8:00:00 AM"
     Next
     For Each objSubFolder In objFolderFSO.SubFolders
             Moodify(objSubFolder.Path)
     Next
End Sub


BUT CANNOT CHANGE DATE CREATED ... resort to powershell

'-----------------------------------------------------------------------------------------------------------------------------------------------
' Can we somehow open and use external .DLL files such as mediainfo.dll
'   or is there an interface altready (eg google it)
'   ... can we even build mediainfo.dll
'


'-----------------------------------------------------------------------------------------------------------------------------------------------
' how can we use ffprobe from vbscript ?
'   can we somehow create a ffprobe .DLL and perhaps use that form vbscript
'


'-----------------------------------------------------------------------------------------------------------------------------------------------
' how can we bulk move source files in vbscript to a different folder
'

' individual file
Set obj = createobject(“Scripting.FileSystemObject”) ‘Creating a File Object
Dim filename1,filename2                                   ‘Declaring variables
filename1=”C:\app\pictures\img1.jpg” ‘Mentioning the name and source location of the file to be moved
filename2=”C:\Users\img1.jpg” ‘Mentioning the name and destination location of the file to be moved
obj.MoveFile filename1,filename1 ‘MoveFile Method is used for moving the file to the destination
Set obj=Nothing                                           ‘Releasing File object


?? bulk move ??
best to use the DOS command 


'-----------------------------------------------------------------------------------------------------------------------------------------------
' how can we delete a file in vbscript
'


'-----------------------------------------------------------------------------------------------------------------------------------------------
' how can we copy the contents of a file in vbscript and deal with special characters for DOS .bat files
'    see file "G:\HDTV\000-TO-BE-PROCESSED\zzz-TEST\-----------------.BAT"
'
'

